# 데이터센터 냉각 RL — 의존성 (현재 학습/평가에 실제 사용된 버전으로 고정)
# 주의: 모델 .zip은 numpy/sb3 버전에 민감하므로, 학습과 평가는 동일 버전에서 수행할 것.

torch==2.12.0
stable-baselines3==2.8.0   # SAC / TD3 / PPO
gymnasium==1.2.3
numpy==2.1.3
pandas==2.2.3
matplotlib==3.10.0
seaborn==0.13.2
tensorboard==2.20.0
tqdm==4.67.1               # progress_bar 사용 시 필요
rich==13.9.4               # progress_bar 사용 시 tqdm와 함께 필요
